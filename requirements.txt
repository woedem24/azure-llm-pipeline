azure-functions==1.21.3
azure-identity==1.17.1
azure-keyvault-secrets==4.8.0
azure-cosmos==4.7.0
# Pinned exactly, like the chatbot project: ">=1.50.0" floats onto any future
# major release, so a breaking change upstream becomes a broken deploy here.
openai==1.51.0
httpx==0.27.2
